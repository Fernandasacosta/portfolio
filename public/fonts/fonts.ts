import {Oswald, Syncopate, Syne} from 'next/font/google'

export const oswald = Oswald ({
  subsets: ['latin'],
  display: 'swap',
})

export const syne = Syne({
  subsets: ['latin'],
  display: 'swap',
})

export const syncopate = Syncopate({
  weight: '700',
  subsets: ['latin'],
  display: 'swap',
})